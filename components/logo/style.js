import { StyleSheet } from "react-360";

export default StyleSheet.create({
   view: {
    transform: [
        {
            translate: [-20, 0, -5]
        }
    ],
    display: "flex",
    backgroundColor: "red",
    width: "100%",
    height: "5em"
   }
})